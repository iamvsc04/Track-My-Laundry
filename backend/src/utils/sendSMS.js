// Example using Twilio
// const twilio = require('twilio');
// const client = twilio('TWILIO_ACCOUNT_SID', 'TWILIO_AUTH_TOKEN');

module.exports = async (to, text) => {
  // await client.messages.create({ to, body: text, from: 'YOUR_TWILIO_NUMBER' });
};
